# 后端环境变量示例（复制为 backend/env 使用）

# 后端监听地址（Docker/云服务器一般用 0.0.0.0）
HOST=0.0.0.0

# 公网 IP（用于启动日志打印；不影响实际监听）
PUBLIC_IP=47.105.118.142

# Redis 连接（Docker Compose 下建议 REDIS_HOST=redis）
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# SSE 调试日志（建议用相对路径，容器/迁移更稳定）
SSE_DEBUG_LOG=./logs/sse_forward.log


